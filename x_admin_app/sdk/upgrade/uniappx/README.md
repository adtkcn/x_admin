# uni-app x 端（uts）

组件自包含，源文件位于 `components/x-upgrade/x-upgrade.uvue`（uni-app x 工程不编译
.vue 后缀，可与 vue3 端组件同目录共存；集成到 uni-app x 工程时拷贝该文件即可）。

与 vue3 端的差异（不支持 wgt、iOS 不处理、uni.installApk 安装）见 ../README.md。
